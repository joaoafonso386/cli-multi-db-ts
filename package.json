{
  "name": "node-life-api",
  "version": "1.0.0",
  "description": "",
  "main": "main.ts",
  "scripts": {
    "clear": "rimraf ./dist/",
    "build": "npm run clear && tsc -p ./tsconfig.json && cp package.json package-lock.json dist/",
    "test:cli": "npx ts-mocha -p ./tsconfig.json src/cli/test/*.test.ts",
    "dev:cli": "tsc -w"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/expect": "^24.3.0",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.10.5",
    "mocha": "^10.2.0",
    "rimraf": "^5.0.5",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.3.3"
  }
}
